name: CI Pipeline
 
on:
  push:
  pull_request:
 
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
 
      - name: Build
        run: echo "✅ Build OK!"
 
      - name: Test
        run: |
          echo "🧪 Testit käynnissä..."
          echo "Testi 1: PASSED"
          echo "Testi 2: PASSED"
          echo "✅ Kaikki testit läpäisty!"
